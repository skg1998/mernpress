const Header = ('../models/Header.model')
const _ = ('lodash')
const errorHandler = ('../util/dbErrorHandler')
const formidable = ('formidable')
const fs = ('fs')

const addHeader = (req, res, next) => {

}

const updateHeader = (req, res, next) => {

}

const deleteHeader = (req, res, next) => {

}

const getHeader = (req, res, next) => {

}

module.exports = {
  addHeader,
  updateHeader,
  deleteHeader,
  getHeader
}